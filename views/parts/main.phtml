<!DOCTYPE html>
<html lang="en" dir="ltr">

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="minimum-scale=1, initial-scale=1, width=device-width, shrink-to-fit=no">
        <meta
            http-equiv="Content-Security-Policy"
            content="
                default-src 'self';
                script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://cdn.jsdelivr.net;
                style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com;
                font-src 'self' https://cdnjs.cloudflare.com;
                frame-src https://www.youtube.com;
                img-src 'self' https://*;
            "
        />
        <meta name="referrer" content="no-referrer" />

        <title><?php echo $view->siteTitle . View::$pageData["pageTitle"]; ?></title>
        <meta name="description" content="Propulsion Hub - Portal 2 Challenge Mode Leaderboards, personal fork of board.portal2.sr.">

        <meta property="og:site_name" content="Propulsion Hub">
        <meta property="og:title" content="<?php echo $view->siteTitle . View::$pageData["pageTitle"]; ?>">
        <meta property="og:description" content="Portal 2 Challenge Mode leaderboard - live score updates, chamber times, and speedrun categories.">
        <meta property="og:image" content="https://p2sr.laveryjonez.uk/images/brand/unkn-logo.png">
        <meta property="og:type" content="website">
        <meta name="theme-color" content="#f7931e">
        <meta name="twitter:card" content="summary">

        <?php foreach($view->css as [$path, $hash]): ?>
            <link rel="stylesheet" type="text/css" href="<?=$path; ?>"<?php echo isset($hash) ? " integrity=\"$hash\" crossorigin=\"anonymous\"" : '' ?>>
        <?php endforeach; ?>

        <?php foreach($view->js as [$path, $hash]): ?>
            <script type="text/javascript" src="<?=$path; ?>"<?php echo isset($hash) ? " integrity=\"$hash\" crossorigin=\"anonymous\"" : '' ?>></script>
        <?php endforeach; ?>

        <link rel="icon" href="/favicon.ico" type="image/x-icon" />
        <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" />
        <script>
            function getCookie(name) {
                const match = document.cookie.match(new RegExp('(^| )' + name + '=([^;]+)'));
                return match ? match[2] : null;
            }
            function setCookie(name, value) {
                const d = new Date();
                d.setTime(d.getTime() + (10 * 365 * 24 * 60 * 60 * 1000)); // 10 years
                document.cookie = name + '=' + value + '; path=/; expires=' + d.toGMTString();
            }

            // Light is the default; dark is opt-in and persisted via cookie
            // (not localStorage) so it's available to server-rendered pages too.
            getCookie('color-theme') === 'dark' && document.documentElement.classList.add('dark');

            function toggleTheme() {
                if (getCookie('color-theme') === 'dark') {
                    setCookie('color-theme', 'light');
                    document.documentElement.classList.remove('dark');
                } else {
                    setCookie('color-theme', 'dark');
                    document.documentElement.classList.add('dark');
                }

                if (location.pathname.startsWith('/changelog') || location.pathname.startsWith('/profile/')) {
                    $(".rank, .preRank, .postRank").each(function() {
                        !this.firstElementChild && drawRank($(this));
                    });
                }
            }
        </script>
    </head>

    <body>
        <div id="wrapper">

            <?php include("announcement.phtml"); ?>
            
            <div id="header">
                <div id="header_left">
                    <img id="brand-logo" src="/images/brand/unkn-logo.png" alt="Propulsion Hub logo">
                    <h2 id="sitetitle"><span class="site-title-propulsion">Propulsion</span> <span class="site-title-hub">Hub</span></h2>
                    <div id="navigation">
                        <?php include("navigation.phtml"); ?>
                    </div>
                    <?php //include("donation.phtml"); ?>
                </div>
                <div id="header_right">
                    <div id="userinfo">
                        <?php include("userinfo.phtml"); ?>
                    </div>
                </div>
            </div>
            <?php include("message.phtml"); ?>
            <div id="content">
                <?php
                    if (isset(View::$pageData["contentTemplate"])) {
                        require_once(__DIR__."/../".View::$pageData["contentTemplate"]);
                    }
                ?>
            </div>
            <div class="push"></div>
        </div>
        <?php include("footer.phtml"); ?>
    </body>

</html>
